meta {
  name: GET messages in thread
  type: http
  seq: 6
}

get {
  url: {{URL}}/threads/:thread_id/messages
  body: none
  auth: inherit
}

params:path {
  thread_id: {{__thread_id}}
}

script:post-response {
  bru.setVar(__message_id, res.getBody()["messages"][0]["id"])
}
