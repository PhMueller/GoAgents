meta {
  name: CREATE message in thread
  type: http
  seq: 5
}

post {
  url: {{URL}}/threads/:thread_id/messages
  body: json
  auth: inherit
}

params:path {
  thread_id: {{__thread_id}}
}

body:json {
  {
    "content": "hello"
  }
}
