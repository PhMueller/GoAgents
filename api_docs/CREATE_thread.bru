meta {
  name: CREATE a thread
  type: http
  seq: 2
}

post {
  url: {{URL}}/threads
  body: json
  auth: inherit
}

body:json {
  {
    "title": "TestTitle"
  }
}

script:post-response {
  bru.setVar("__thread_id", res.body.id)
}
